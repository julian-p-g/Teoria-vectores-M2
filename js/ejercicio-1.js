//crea un vector vacio llamado colores luego agrega 3 colores diferentes y recorre el vector

let colores = []

colores.push("verde", "azul", "amarillo");

for(let i = 0; i < colores.length; i++) {
    console.log(colores[i]);
}

console.log(colores);
